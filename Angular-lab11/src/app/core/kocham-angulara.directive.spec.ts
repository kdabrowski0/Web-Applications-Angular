import { KochamAngularaDirective } from './kocham-angulara.directive';

describe('KochamAngularaDirective', () => {
  it('should create an instance', () => {
    const directive = new KochamAngularaDirective();
    expect(directive).toBeTruthy();
  });
});
